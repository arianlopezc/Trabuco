package {{.GroupID}}.worker.config;

import org.springframework.context.annotation.Configuration;

/**
 * JobRunr configuration.
 *
 * <p>JobRunr is configured via application.yml properties:
 * - jobrunr.background-job-server.enabled: true
 * - jobrunr.dashboard.enabled: true
 *
 * <p>JobRunr automatically detects and uses the configured datasource
 * (PostgreSQL, MySQL, or MongoDB) for job persistence.
 *
 * <p>For advanced configuration, you can customize JobRunr by providing
 * beans of type:
 * - JobRunrConfigurationCustomizer
 * - BackgroundJobServerConfigurationCustomizer
 * - DashboardConfigurationCustomizer
 */
@Configuration
public class JobRunrConfig {

  // JobRunr is auto-configured via Spring Boot starter
  // Add any custom configuration here if needed

}
